import React from "react";
import "./style.css";

export default function Header() {
    return (
        <div className="headerBg">
            <h2>Click on a player to earn points, but don't click on any more than once!</h2>
        </div>
    );
}